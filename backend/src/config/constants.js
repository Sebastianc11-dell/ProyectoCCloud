const roles = {
  ADMIN: 'admin',
  PREMIUM: 'premium',
  USER: 'user'
};

const productStatus = {
  ACTIVE: 'active',
  INACTIVE: 'inactive'
};

const alertChannels = {
  EMAIL: 'email',
  SMS: 'sms'
};

module.exports = {
  roles,
  productStatus,
  alertChannels
};
